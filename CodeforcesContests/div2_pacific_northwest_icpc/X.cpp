#include <bits/stdc++.h>
using namespace std;
#include <math.h>

int main(void){
    int S;
    int max_stars_per_row = round(sqrt(S));
    set<pair<int,int>> arrangements;

    for(int i = 2; i <= max_stars_per_row; ++i){
        
    }
}